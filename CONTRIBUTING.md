# Contributing

Keep changes small, typed, test-covered, and reversible. Add an ADR for a
provider dependency, a security-boundary change, or a change to authority,
financial controls, data classification, or retention.

Before opening a pull request run:

```bash
pnpm typecheck && pnpm test && pnpm build
```
